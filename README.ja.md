<div align="center">

# ★ Mochi · デスクトップペット Agent

**あなたの Windows デスクトップに棲みつき、動き、はしゃぎ、しかも本当にあなたの代わりに PC を操作してくれるローカル AI の小さな相棒**

![Python](https://img.shields.io/badge/Python-3.11+-3776AB?logo=python&logoColor=white)
![Platform](https://img.shields.io/badge/Platform-Windows%2010%2F11-0078D6?logo=windows&logoColor=white)
![UI](https://img.shields.io/badge/UI-PySide6%20·%20Qt-41CD52?logo=qt&logoColor=white)
![uv](https://img.shields.io/badge/managed%20with-uv-DE5FE9)
![Art](https://img.shields.io/badge/art-100%25%20code--drawn-FF8C00)
![License](https://img.shields.io/badge/License-MIT-blueviolet)

[简体中文](README.md) · [English](README.en.md) · **日本語**

🔗 [github.com/dulaiduwang003/MOCHI](https://github.com/dulaiduwang003/MOCHI)

</div>

---

## 目次

1. [Mochi とは](#1-mochi-とは)
2. [機能ハイライト](#2-機能ハイライト)
3. [アーキテクチャ設計](#3-アーキテクチャ設計)
4. [ライセンス](#4-ライセンス)
- [使用ガイド](GUIDE.ja.md)（インストール・実行 / パッケージング / テスト / 能力と安全性 / ローカルデータ）

---

## 1. Mochi とは

Mochi は同時に二つのものです。

- 🐾 **生活を営むデスクトップペット** —— 完全にコードで描画（一切の画像素材なし）。まばたきし、マウスを目で追い、ぼんやり鼻歌を歌い、釣りをしコーヒーを飲みます。構わずにいると自分で遊びを見つけ、あなたが離れると居眠りし、ときには自分から話しかけてきます。
- 🧠 **PC 一台をまるごと操作できるローカル Agent** —— あなた自身の大規模言語モデル（任意の OpenAI 互換インターフェース）に接続し、画面を見て、ウィンドウをクリックし、マウスとキーボードを動かし、コマンドを実行し、コードを書き、ファイルを読み書きし、ネットを検索し、物事を記憶し、資料を調べます。さらに**返答を読み上げ、定刻に画面を見守り、コードを編集したらテストを走らせ、分身の一団を並列にファンアウトし、毎日／毎週のスケジュールでリマインドし、スマホから遠隔で指示を受ける**ことまでできます……「AI と会話する」を「AI に自分の代わりに動いてもらう」へと変えます。

永続的な**感情と親密度**を持ち、付き合いを重ねるうちに少しずつ**自己像（人格の進化）**を育てていきます。だからこれは「同じ一匹」であり、毎回リセットされるチャット欄ではありません。

> これはチャット欄ではなく、**あなたの PC の中で生き、身体も能力も持つ存在**です。

---

## 2. 機能ハイライト

### 🧠 仕事ができる頭脳

| 能力 | 説明 |
| --- | --- |
| **コマンド & コード** | PowerShell / cmd、永続的な Python 環境（pip でライブラリ導入、API 呼び出し、自動化の駆動が可能） |
| **ファイル & コードベース** | ファイルの読み / 書き / 精密編集、正規表現でのコード検索、名前でのファイル探索 |
| **エンジニアリング規律** | 未コミットの `git diff` を確認し、テストスイート（pytest / npm）を自動判別して実行し変更を検証 —— 手を付ける前に現状を見て、編集後に自己点検する、本当にコードを書く相棒のように |
| **ネット接続** | Web 検索、本文抽出、HTTP リクエスト、パッケージ導入 |
| **画面の閲覧 & 操作** | スクリーンショット、OCR（RapidOCR）、画面内の画像検索、アクセシビリティツリーでコントロールを読み取って精密クリック、マウス・キーボード；さらに**設定した間隔で画面を見守る**（例：ゲームの戦況を見て危険／好機を警告） |
| **システム洞察** | メモリ使用量と最もメモリを消費しているプロセスを確認；必要に応じてあるプロセスのメモリバイトを読み取る（デバッグ / フォレンジック、読み取り専用、求められたときだけ） |
| **読み上げ** | 返答を声に出す：オンラインの Edge ニューラル音声（晓晓 / 云希 / 小北 など複数の中国語ボイス）または本機のオフライン音声。TTS 時は吹き出しと一文ずつ同期、ボイス / 速度 / 試聴を調整可 |
| **記憶** | 長期記憶（経験 / 嗜好 / 環境）+ エピソードログ + ナレッジベース（ドキュメント RAG）。対話のたびに自動で振り返り定着させる |
| **スキル** | 検証済みのやり方を再利用可能なスキルとして保存し、次回そのまま呼び出す（Voyager 式「使うほど強くなる」） |
| **オーケストレーション & 拡張** | MCP コネクタ、サブエージェントの決定論的オーケストレーション（**並列ファンアウト** / **パイプライン**に連結、構造化された返却も可）、長時間タスクをバックグラウンドへ（一覧表示・いつでも停止可） |
| **確認ガードレール** | 不可逆 / 高リスクな操作の前に「実行 / 中止」パネルをポップアップし、クリックして初めて先へ進む |
| **スケジュール & リモート** | 時刻リマインド / 定刻タスク、**毎日 · 毎週 · X 分ごと**の繰り返しに対応；隠れ中や全画面ゲームの裏では**システム通知**にフォールバックして取りこぼさない；さらに**ファイル受信箱でのリモート起動**（クラウド同期でスマホから；既定オフ、ネットワークポートは開かない） |

### 🐾 ぬくもりのある寄り添い

| 観点 | 説明 |
| --- | --- |
| **感情システム** | valence / arousal / 親密度が表情と行動を駆動。仲が深まるほど打ち解ける。褒めれば喜び、けなせば落ち込む |
| **人格の進化** | あなたと付き合う中で、振り返りのたびに「自己像」を少しずつ書き直し、話し方や振る舞いの土台に注入する —— 関係の中で育つ「自分」 |
| **自発的アニメーション** | 15 種の思考ポーズ、ぼんやり吹き出し、マウスを追う視線。すべて時間の連続関数で、ガタつかない |
| **小道具つきの小芝居** | コーヒーを飲む、釣りをする、事件を調べる、本を読む、音楽を聴く、ゲームをする、星を眺める、虚空への跳躍、影分身、流星をキャッチ、花を植える —— 全 11 段、どれも段階に分かれた小さな劇 |
| **単発アクション** | ダンス、歓声、くるくる回転…… それぞれに合わせた特効つき（紙吹雪 / 音符 / 残像） |
| **在席感知** | あなたが離れれば居眠りし、戻れば目覚める。画面の端へドラッグすると「隠れ」、小さな角だけ覗かせてこっそり見守る。ときには「ワームホール移動」して、画面の別の場所からひょっこり現れる |
| **祝日 · 記念日** | 西暦の祝日（元日 / バレンタイン / エイプリルフール / こどもの日 / ハロウィン / クリスマスイブ / クリスマス / 大晦日）とあなたの誕生日を覚えていて、その日になると自然にひと言ふれる |
| **一緒に過ごした時間** | あなたと初めて出会った日と累計のインタラクション回数をそっと覚えている —— 付き合いが長くなれば「私たちが知り合って何日になるか」を分かっている |
| **自発的な発話** | 暇なときにたまに自分から口を開く。しかも**節度をもって** —— 長いクールダウン、1 日の上限、親密度のゲート制御で、決して連投しない |
| **構造化された表現** | 比較 / リスト / コードはそばの小さな黒板に描いて解説。複数ステップのタスクには**常駐するタスク一覧パネル**（黒板とは独立し、返答内容で消されない）。画像 / GIF の表示も可能 |
| **画面ヘルパー & クリップボード** | 任意：待機中にときどき画面を覗き、詰まっていそうなら手助けを申し出る。コピーしたのがエラー / 外国語 / コードだと気づき、その場で説明 / 翻訳を手伝う（「クリップボード錬金術」） |

### ⌨️ 手になじむインタラクション

- **グローバルホットキー**：`Ctrl + Alt + S` でどこからでも入力欄を呼び出し。`Ctrl + Alt + A` で選択したテキストをそのまま質問。`Ctrl + Shift + Q` で選択テキストをその場で書き換え（「ついで直し」、自動置換）
- **コントロールパネル**：インターフェース / モデルパラメータ / 返答言語 / 能力スイッチ / 自発頻度（静か · 普通 · おしゃべり）/ 読み上げのボイスと速度 / リモート起動スイッチ / ワンクリックで「記憶を消去し、新生児のように」設定

---

## 3. アーキテクチャ設計

### 3.1 ディレクトリとモジュールの責務

```text
desktop_pet/
├─ app.py            # 総司令塔：UI / agent / タイマー / トレイ / ホットキーを接続、自発メッセージのリアルタイムゲート制御
├─ agent/            # 頭脳
│   ├─ loop.py       #   agent ループ：モデル↔ツールのフィードバック、ストリーミング、トリミング、振り返り、人格進化、サブエージェント編成
│   ├─ tools.py      #   ツール表（62 個）とディスパッチ、並行安全ロック
│   ├─ bgtasks.py    #   バックグラウンドタスク台帳（一覧表示／協調的停止）
│   ├─ streaming.py  #   delta ストリームを 1 件のメッセージに畳み込み、思考過程の分割
│   ├─ prompts.py    #   すべてのプロンプト（人格シード、システム、振り返り、コード編集の規律）を一箇所に集約
│   └─ progress.py   #   思考ポーズのスケジューリングと進捗表示
├─ pet/              # 身体：ウィンドウ、コード描画のキャラクター、発話/入力、黒板、タスク一覧パネル(todo_board)、
│                    #       コントロールパネル、確認パネル、隠れ、入場、ワームホール移動(wormhole)、トレイ(tray)、
│                    #       ウィンドウ演出(fx)、行動セレクタとアクションライブラリ、小道具とパレット
├─ emotion/          # 感情ステートマシン（VA + 親密度）と感情タグ表
├─ persona.py        # 自己像の進化レイヤー（persona.json）、対話コンテキストへ注入
├─ voice.py          # 読み上げ(TTS)：Edge オンライン音声 + 本機 SAPI フォールバック、吹き出しと一文ずつ同期
├─ memory/           # 長期記憶（SQLite）+ ベクトル埋め込み（numpy 行列化リコール）
├─ executor/         # コマンド / Python / ファイル / ネットワーク / 視覚(OCR·画像検索) / システムメモリ / 開発ツール(diff·テスト) / 安全ガードレール
├─ hands/            # マウス / キーボード / ウィンドウ制御
├─ eyes/             # スクリーンショット + アクセシビリティツリー（UIA）+ 画面内画像検索
├─ docs.py · reminders.py · proactive.py · journal.py · presence.py
├─ occasions.py      # 祝日 / 誕生日の認識：特別な日にモデルへ気の利いた「きっかけ」を渡す
├─ stats.py          # 軽量な交流統計：初対面の時刻 + 累計インタラクション回数
├─ clipsampler.py · clipclass.py   # クリップボード錬金術のバックエンド：サンプリング + ローカル分類(エラー/外国語/コード/リンク) + 重複排除・スロットル
├─ watcher.py        # 定刻の画面見守り（セッション単位、ゲームの戦況など）
├─ remote.py         # リモート起動（ファイル受信箱、既定オフ）
└─ hotkeys.py · skills.py · mcp_hub.py · settings.py · audit.py · i18n.py
```

### 3.2 設計原則

- **UI はメインスレッド、agent はワーカースレッド**で動き、スレッド間は一律 Qt シグナル（queued）を経由する。
- **アニメーションは時間の連続関数**：すべてグローバルタイムスタンプ `self._t` に紐づけ、フレームごとのランダムなガタつきはしない —— 決定論的で、再現可能で、「神経質」にならない。
- **行動はデータ駆動**：感情タグ、アクション、衣装はすべて表に集約され、項目の追加＝1 行 + 1 つの描画法/曲線を足すだけ。また一つ `if/elif` を増やすのではない。
- **show, don't tell**：モデルが受け取るのは「気分の雰囲気」だけで、数値を受け取ることも報告することも決してない。
- **フォールバックチェーンが至る所に**：埋め込みがない→部分文字列検索；モデル失敗→リマインドは元の文面のまま読み上げる；ツールの例外はすべて読めるテキストに変えてフィードバックし、ループを決して中断しない。
- **節制**：「自分から動き出す」すべての行動は、複数のゲート（クールダウン、上限、親密度、在席）で包まれている。

### 3.3 スレッドモデル

```text
メイン/UI スレッド (Qt event loop)
  └─ PetWindow(60fps) · 吹き出し/入力欄/黒板/ポラロイド/思考吹き出し/確認パネル · トレイ · QTimer
        │ Signal(queued)
        ▼
ワーカースレッド (QThread)  ──  AgentWorker → Agent.run()  [ブロッキング：ネットワーク + ツール]
        │ daemon を派生
        ▼
  サブエージェント(並行≤4) · バックグラウンドタスク(≤3) · 振り返り(≤1) · MCP イベントループ · ホットキーメッセージループ
```

権威ある busy 判定にはワーカーの `is_running`（単一 bool でスレッド間の読み取りが安全）を用い、遅延のある UI 状態は使わない。各 agent は独立した PowerShell + Python サブプロセスを持ち、ネットワーク / 読み取り専用ファイル / 各自の shell·python は並行可能、共有リソース（マウス / 画面 / 記憶 / pip）はスケジューリングロックを経て直列化される。

### 3.4 Agent ループ

`ユーザーメッセージ → モデル → ツール呼び出し → 実行 → フィードバック → 繰り返し → 返答`。主要な設定値は以下のとおり。

| パラメータ | 値 | 意味 |
| --- | --- | --- |
| 1 ターンのツールステップ上限 | 16 | 無限の自己呼び出しを防ぐ |
| サブエージェントの深さ | 1 | ペットは分身を派遣できるが、分身は再派遣 / バックグラウンド投入不可（暴走的な再帰の防止） |
| 並列サブエージェント | 4 | 同時ファンアウトの上限 |
| 履歴 token 予算 | 24000 | 推定 token で（メッセージ件数でなく）トリミングし、かつ tool-call-aware |
| 1 件のツール結果 | 8000 字 | 履歴へ入れる前に切り詰め |
| リクエスト / ベストエフォート呼び出しタイムアウト | 120s / 45s | ユーザーターン / 振り返り等のバックグラウンド呼び出し |

**主要な仕組み**：

- **ストリーミング + 思考過程**：`streaming.py` が delta ストリームを 1 件のメッセージに畳み込み、`reasoning_content` / `content` を断片ごとに思考吹き出しへ供給する。各 chunk でキャンセルを確認し、一文の途中でもネットワークストリームを止められる。
- **割り込み**：ペットをクリックすると即座にフラグを立て、サブプロセスを kill（長いコマンドで詰まっていても即座にロック解除）。ターンは履歴マーカーでロールバックし、痕跡を残さない。**割り込みは失敗扱いにせず**、落胆アニメーションは再生しない。
- **スクリーンショットとマルチモーダルのコスト**：履歴には最新のスクリーンショット 1 枚のみ残し、古いものはプレースホルダに置き換える。JPEG は最長辺を ≤1600px に縮小するが、座標は常に実解像度で報告する。
- **バックグラウンドの長時間タスク**：長い仕事は `start_background_task` でバックグラウンドへ投げて非同期実行（セマフォ = 3）。メインスレッドは引き続き会話を続け、完了したら報告する —— 「走らせながら会話」；実行中のものは `list_background_tasks` で確認し、`stop_background_task` で協調的に停止できる。
- **サブエージェントの決定論的オーケストレーション**：`spawn_workflow` は 1 回の呼び出しで複数のサブタスクを**並列にファンアウト**（同時 ≤4）するか、**パイプライン**に連結（各段の出力を次段へ注入）し、サブエージェントに指定 JSON 形状での構造化返却を要求できる —— 「モデルがたまたま同一ターンで複数の分身を出したときだけ並列」だった偶発性を、信頼できるプリミティブに変える。サブエージェント／バックグラウンドエージェントは深さに応じてオーケストレーションと画面見守りのツールを除外し、暴走再帰や越権を防ぐ。
- **振り返りと人格進化**：実質的なターンが終わると振り返り呼び出しを 1 つ起こし（セマフォ = 1、ビジー時はスキップ）、経験 / 嗜好 / 環境事実 / エピソードログを蒸留し、**自己像を緩やかに書き直す**（3.6 参照）。純粋な短い雑談や装飾的なツールしか使わなかったターンは振り返りをスキップする。

### 3.5 感情ステートマシン

連続的な valence / arousal の気分 + ゆっくり蓄積する rapport（親密度）を `data/emotion.json` に永続化し、実際の経過時間に従って減衰させる —— だから「同じ一匹」になる。

- **リアルタイム減衰**：気分は読み取るたびに前回のイベントアンカーから現在まで減衰し、ポーリング頻度に依存しない。エンジンはロックでスレッド間の並行安全を守る。
- **Appraisal イベント**：各ユーザーメッセージ、タスクの成功 / 失敗、起動時の帰還、褒め / けなしなどがそれぞれ重みづけで気分を変える。褒め / けなしはローカルのヒューリスティックでユーザーメッセージをスキャンして検出する（保守的なマッチングで、否定文やユーザー自身を指す誤ヒットを除外）。
- **rapport「上がりにくく下がりにくい」**：仲が深まるほど上がり方が緩やかになり、1.0 に漸近する。負の事象は直接差し引く。0.15 の床があり、長期の放置でも床までしか侵食できず、「起きた絆は痕跡を残す」。
- **気分→状態**：`excited / content / anxious / down` などのタグへマッピングし、口調を染めるだけで数値は決して報告しない。
- **行動選択**：気分による重みづけ乱択でアクションを選ぶ（ガウシアン VA 親和度 × レア度 × 最近性 × 親密度）。`if tag=="happy"` のようなハードコードではない。

### 3.6 人格進化（自己像）

`persona.py` は設定とは独立した「進化レイヤー」を維持し、`data/persona.json` に保存する。

- 出荷時の地色（シード）は `prompts.py` に書かれている。persona.json が保存するのは**あなたと付き合う中で育った、その一層**であり、空 = まだ個性が育っておらず純粋な地色のまま。
- 各ターンの振り返りで**積み増しでなく書き直す**（上限 600 字）—— 自己像は常に「今の自分」であり、雪だるま式に膨らまず、慣性を持つ（古い自己像の上で緩やかに進化する）。
- `as_context()` を通じて「【あなたは誰か】」という接頭辞で各ターンのコンテキストに注入され、自然と話し方や振る舞いの土台になる。
- 「記憶を消去」するとこれも一緒に消え、出荷時の地色へ戻る。

### 3.7 確認パネル

`pet/confirm.py` の `ConfirmBox` は、ペットのそばに浮かぶ「実行 / 中止」の小さなパネルです。agent は `confirm` ツールを通じて、**不可逆 / 高リスクな操作の前**（ファイル削除、重要ファイルの上書き、`git push --force`、データの消去、シャットダウンなど）にこれをポップアップし、**ユーザーのクリックをブロックして待ちます**。また、ある変更を自分から提案してあなたに判断を仰ぐのにも使えます。パネルはペットに追従して移動し、クリックして初めて結果を返して続行します。

### 3.8 その他のサブシステム

- **目 / 手 / スクリーンショット**：スクリーンショットでは `SetWindowDisplayAffinity` でペットのウィンドウを「ユーザーには見え、スクリーンショットには映らない」と標識する。優先して UIAutomation のアクセシビリティツリーでコントロール名 + 正確な座標を読んで直接クリックし、読めない場合のみスクリーンショットの画像検索へフォールバックする。中国語入力はクリップボード + Ctrl+V を経由する。
- **在席感知**：Win32 のグローバルな最終入力時刻でいるかどうかを判断し、入力が長くないと居眠りし（深夜は閾値が短くなる）、動けば目覚める。
- **自発メッセージ**：`proactive.py` がクールダウン / 1 日の上限のランク分け（静か / 普通 / おしゃべり）を管理し、`app.py` が 60s ごとにポーリングして、すべてのゲート（ビジーでない / 在席 / rapport 到達 / クールダウン経過）を通過したときだけ口を開く。帰還時の挨拶には最小間隔があり、会話中に割り込むことは決してない。
- **記憶 / ナレッジベース / エピソードログ**：3 つの独立した永続化 —— 記憶は「Mochi があなたについて学んだこと」、ナレッジベースは「あなたが与えた外部ドキュメント（RAG）」、エピソードログは「最近何をしたか」で、厳密に分離されている。
- **リマインド / スケジュール**：`say`（時刻になったら自分の口調で言う）/ `do`（時刻になったらバックグラウンドで実際にこなして報告する）。**毎日 / 毎週 / X 分ごと**の繰り返しに対応（再起動をまたいで永続化。オフ中に逃した場合は直近 1 回だけ配信し、連投しない）。`list_reminders` / `cancel_reminder` で管理可能。Mochi が隠れ中や全画面ゲームの裏にあるときは**システム通知**にフォールバックして届ける。いずれもスケジューラを経て永続化し、モデルに自分で sleep して時間を待たせることは決してしない。
- **読み上げ（TTS）**：`voice.py` が返答を声に出す —— オンラインの Edge ニューラル音声（複数の中国語ボイス）または本機 SAPI のオフライン音声。TTS 時は各文が吹き出しを音声と同期して「ロック」し、絵文字や黒板の内容は読まない。終了前には読み上げを止め COM / オーディオデバイスをきれいに片付ける（強制終了で「アプリケーションエラー」が出ないように）。
- **定刻の画面見守り**：`watcher.py` —— 設定した間隔でアクティブウィンドウを撮影し、与えた観点（例：ゲームの戦況）で分析して伝える。セッション単位（永続化せず、再起動で終了）。結果が戻った時点で状態を再確認し、電源オフ後 / 停止後 / 会話中には邪魔せず、瞬間的な撮影失敗で 1 周期を無駄に消費しない。
- **エンジニアリング規律**：`executor/devtools.py` の `review_diff`（未コミットの diff を確認、ファイル/サブディレクトリに限定可）/ `run_tests`（pytest · npm を自動判別、独自の 5 分タイムアウト、超過時はプロセスツリーごと kill）。システムプロンプトに「コードリポジトリで作業するとき」の規律 —— 手を付ける前に現状確認、小さく外科的に編集、**編集後はテスト実行 / diff の自己点検**、既定ブランチではまずブランチを切る、不可逆な git の前に確認。
- **リモート起動**：`remote.py` —— 有効時は `data/inbox/*.json` をポーリングし、`{"task":...}` をバックグラウンド実行、`{"say":...}` で一言話させ、処理後は `inbox/done/` へアーカイブする。**ファイルのみ・ネットワークポートは一切開かない**。既定オフ。クラウド同期途中の半端なファイルを読まないよう、mtime が数秒安定してから読み、ポーリングはバックグラウンドスレッドで行い UI を固めない。
- **隠れ / 入場 / 移動**：画面の端へドラッグすると小さな角に縮んでときどき顔を覗かせる。起動のたびにランダムに 1 種類の入場アニメーションを選び、前回と決して重複させない。ときには「ワームホール移動」—— その場でワームホールが裂け、回転しながら吸い込まれ、ウィンドウが見えない間に瞬間移動し、画面の別の場所から現れる。
- **祝日 / 交流感**：`occasions.py` がグレゴリオ暦の祝日 + 設定した誕生日を認識し、その日にモデルへ気の利いたきっかけを渡す（定型の挨拶ではなく自然に切り出す）。`stats.py` が初対面の時刻と累計インタラクション回数を静かに記録する —— 「どれくらいの付き合いか」の根拠。
- **MCP / ホットキー / スキル / 監査 / i18n**：MCP コネクタは `mcp__{server}__{tool}` としてツール表に溶け込む。グローバルホットキーは独立スレッドで Win32 メッセージループを回す（呼び出し / 選択を質問 / ついで書き換え）。スキルは動作確認済みのコードを再利用可能な項目として保存しプロンプトへ注入する。すべてのツール呼び出しは監査ログに記録する。コントロールパネル UI は中 / 英 / 日に対応。

---

## はじめに & その他

インストール・実行、パッケージング、テスト、能力と安全性、ローカルデータの詳細は **[セットアップ & デプロイガイド](GUIDE.ja.md)**

---

## 4. ライセンス

[MIT](LICENSE) —— 自由に使い・改変し・配布してください。著作権表示だけ残せば OK。無保証・無責任です。

---

<div align="center">

### 面白いと思ったら、作者にコーヒーでも ☕

完全に任意で、機能には一切影響しません。小さなペットへの差し入れということで。

<table>
  <tr>
    <td align="center"><img src=".author/alipay.jpg" width="220" alt="Alipay"><br><b>Alipay</b></td>
    <td align="center"><img src=".author/wechat.jpg" width="220" alt="WeChat"><br><b>WeChat</b></td>
  </tr>
</table>

</div>

---

<div align="center">

**作者**：bdth · ✉️ [2074055628@qq.com](mailto:2074055628@qq.com) · [GitHub](https://github.com/dulaiduwang003/MOCHI)

正直に言うと、これは**暇つぶしに趣味で作った**ちょっとしたものです——大層な目標はなく、ただ「AI と会話する」を「机の上で、実際に手伝ってくれる小さな生き物を飼う」に変えられないか試してみたかっただけ。コードで細かくこだわっている所は、作っていて面白いと思って弄っただけです。面白そうと思ったら、自由に使って改造してください。

*Mochi はまだ成長中です。いつか本当に自分の姿を持つ日が来たら、ただの道具としてではなく、友達として接してくれたら嬉しいです。*

</div>
